spring.application.name=mercado

# Configura��o do Banco H2 - ATIVADA

# URL de conex�o para um banco em mem�ria chamado 'testdb'
spring.datasource.url=jdbc:h2:mem:testdb

# Driver do H2 (geralmente n�o precisa, mas � bom ter)
spring.datasource.driverClassName=org.h2.Driver

# Usu�rio e senha padr�o do H2
spring.datasource.username=sa
spring.datasource.password=

# Dialeto do Hibernate para o H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect 

# Permite que o Hibernate crie/atualize as tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update

# Habilita o console web do H2 para voc� visualizar o banco
spring.h2.console.enabled=true

# Define o caminho para acessar o console (ex: http://localhost:8080/h2-console)
spring.h2.console.path=/h2-console